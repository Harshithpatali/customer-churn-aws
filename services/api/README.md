# API Service Boundary

The production FastAPI application currently lives under `api/` to preserve the existing ECS Docker contract.

This directory documents the service boundary for future extraction into an independently deployable API service. The current deployment intentionally remains unchanged.
